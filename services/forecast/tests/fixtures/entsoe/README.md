Hand-written from the documented IEC 62325 format, NOT recorded from the live API (network access to
web-api.tp.entsoe.eu wasn't available when these were written). Replace with recorded responses
(token removed) once the ENTSO-E token is available, and keep the tests passing.
